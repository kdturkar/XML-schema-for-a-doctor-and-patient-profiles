# XML-schema-for-a-doctor-and-patient-profiles

● It's a XML schema for a doctor and patient profiles. Where there will be
multiple doctors and patients data in the XML.

● XML is validated against the schema.

● Transformation of XML to HTML is done using client side XSLT where resulting HTML is well formed and styled using CSS

● Java DOM is created to parse data from XML using XPATH.
